mod signature_parsers;
mod type_env_modifier_parsers;
mod ast;

use crate::lexer::token::Token;

use self::ast::ASTNode;

fn parse(tokens: Vec<Token>) -> Vec<ASTNode> {
    let ast = Vec::new();

    
}